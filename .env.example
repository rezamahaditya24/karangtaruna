# Copy this file to .env and set your API key locally for development.
# Do NOT commit .env to git.
# If you run Cloudflare Wrangler, use `wrangler secret put GEMINI_API_KEY` instead of putting the secret in wrangler.toml.
GEMINI_API_KEY=
